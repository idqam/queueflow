apiVersion: v1
kind: Service
metadata:
  name: api-server
  namespace: queueflow
spec:
  type: ClusterIP
  selector:
    app: api-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 9090
